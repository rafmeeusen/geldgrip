services:
  budget-planner:
    build: .
    ports:
      - "8084:8000"
    volumes:
      - budget-data:/data
    restart: unless-stopped
    environment:
      - DATABASE_URL=sqlite:////data/budget.db

volumes:
  budget-data:
